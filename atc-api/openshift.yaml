---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: atc-api
  labels:
    app: atc-api
spec:
  replicas: 1
  selector:
    matchLabels:
      app: atc-api
  template:
    metadata:
      labels:
        app: atc-api
    spec:
      containers:
        - name: atc-api
          image: atc-api:latest
          ports:
            - containerPort: 8080
          env:
            - name: LLAMA_STACK_URL
              value: "http://localhost:8321"   # override with your remote URL
            - name: WHISPER_BASE_URL
              value: "http://localhost:11434/v1"   # override with your remote URL
            - name: WHISPER_MODEL
              value: "whisper-large-v3-turbo-quantized"
            - name: MONGO_URI
              value: "mongodb://mongodb:27017"
            # Uncomment and populate if your Whisper endpoint requires an API key:
            # - name: WHISPER_API_KEY
            #   valueFrom:
            #     secretKeyRef:
            #       name: whisper-secret
            #       key: api-key
          livenessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 15
          readinessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 10
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
---
apiVersion: v1
kind: Service
metadata:
  name: atc-api
spec:
  selector:
    app: atc-api
  ports:
    - port: 80
      targetPort: 8080
---
apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: atc-api
spec:
  to:
    kind: Service
    name: atc-api
  port:
    targetPort: 8080
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect
